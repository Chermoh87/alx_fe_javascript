# Dynamic Quote Generator

## 📌 Project Overview
The **Dynamic Quote Generator** is a JavaScript-based web application built to demonstrate advanced DOM manipulation, web storage, JSON handling, content filtering, and simulated server synchronization.

This project is part of the **ALX Front-End JavaScript curriculum** and focuses on building interactive, persistent, and scalable client-side applications without using frameworks.

---

## 🎯 Features

### Task 0 – Dynamic Content Generation
- Dynamically displays random quotes
- Allows users to add new quotes and categories
- Uses advanced DOM manipulation techniques
- Event-driven user interactions

### Task 1 – Web Storage & JSON Handling
- Persists quotes using `localStorage`
- Temporarily stores session data with `sessionStorage`
- Supports exporting quotes as JSON
- Supports importing quotes from JSON files

### Task 2 – Dynamic Content Filtering
- Filters quotes by category
- Dynamically populates category dropdown
- Saves and restores last selected category using local storage

### Task 3 – Server Sync & Conflict Resolution
- Simulates server interaction using a mock API (JSONPlaceholder)
- Periodically syncs local data with server data
- Implements conflict resolution where server data takes precedence
- Provides UI notifications for sync updates

---

## 🛠️ Technologies Used
- HTML5
- Vanilla JavaScript (ES6+)
- Web Storage APIs (localStorage & sessionStorage)
- JSON
- Fetch API

---

## 📂 Project Structure
